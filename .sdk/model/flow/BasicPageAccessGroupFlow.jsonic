# BasicPageAccessGroupFlow

main: kit: flow: BasicPageAccessGroupFlow:
{
  entity: "page_access_group",
  kind: "basic",
  name: "BasicPageAccessGroupFlow",
  step: [
    {
      input: {
        ref: "page_access_group_ref01"
      }

      match: {
        page_id: "page01"
      }

      op: "create"
    }

    {
      match: {
        page_id: "page01"
      }

      op: "list",
      valid: [
        {
          apply: "ItemExists",
          def: {
            ref: "page_access_group_ref01"
          }
        }
      ]
    }

    {
      data: {
        page_id: "page01"
      }

      input: {
        ref: "page_access_group_ref01",
        srcdatavar: "page_access_group_ref01_data",
        suffix: "_up0",
        textfield: "created_at"
      }

      op: "update",
      spec: [
        {
          apply: "TextFieldMark",
          def: {
            mark: "Mark01-page_access_group_ref01"
          }
        }
      ]
    }

    {
      input: {
        ref: "page_access_group_ref01",
        srcdatavar: "page_access_group_ref01_data",
        suffix: "_dt0"
      }

      match: {
        id: "page_access_group01",
        page_id: "page01"
      }

      op: "load",
      valid: [
        {
          apply: "TextFieldMark",
          def: {
            mark: "Mark01-page_access_group_ref01"
          }
        }
      ]
    }

    {
      input: {
        ref: "page_access_group_ref01",
        suffix: "_rm0"
      }

      match: {
        id: "page_access_group01",
        page_id: "page01"
      }

      op: "remove"
    }

    {
      input: {
        suffix: "_rt0"
      }

      match: {
        page_id: "page01"
      }

      op: "list",
      valid: [
        {
          apply: "ItemNotExists",
          def: {
            ref: "page_access_group_ref01"
          }
        }
      ]
    }
  ],
  "key$": "BasicPageAccessGroupFlow"
}